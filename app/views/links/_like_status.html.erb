<% if current_user %>
  <% if link.liked_by?(current_user) %>
    Liked <%# TODO: ability to unlike %>
  <% else %>
    <%= button_to "I'd read this article", { controller: :likes, action: :create, item_klass: 'Link', item_id: link.id } %>
  <% end %>
<% end %>
