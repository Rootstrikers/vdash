$(document).ready(function(){
	var modal = $('#article-suggestion-modal'),
        form_container = modal.find('#form-container');
        
	var form_container = modal.find('#form-container');
	$(form_container).html("<%= escape_javascript(render(:partial => 'links/form')) %>");
});