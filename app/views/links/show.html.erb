<div class="pull-right">
  <%= link_to 'All Links', links_path, class: 'btn btn-primary' %>
  <% if @link.user == current_user %>
    <%= link_to 'Edit', edit_link_path(@link), class: 'btn' %>
    <%= link_to 'Delete', link_path(@link), method: 'delete', data: { confirm: 'Are you sure you want to destroy this link? This cannot be undone.' }, class: 'btn btn-danger' %>
  <% end %>
</div>

<h2>
  <%= link_to @link.display_name, @link.url %>
</h2>

<p>
  Submitted by <%= link_to @link.user.name, @link.user %>
</p>

<p>
  <%= @link.summary %>
</p>

<h3>
  Tweet Submissions
  <%= link_to 'Submit New Tweet', new_link_twitter_content_path(@link), class: 'btn pull-right' %>
</h3>

<% if @twitter_contents.any? %>
<ul class="twitter-contents">
  <%= render partial: 'base_contents/content', collection: @twitter_contents, as: :content %>
</ul>
<% else %>
  <p>There aren't any yet. You should probably submit one (or two).</p>
<% end %>

<h3>
  Facebook Submissions
  <%= link_to 'Submit New Post', new_link_facebook_content_path(@link), class: 'btn pull-right' %>
</h3>

<% if @facebook_contents.any? %>
<ul class="facebook-contents">
  <%= render partial: 'base_contents/content', collection: @facebook_contents, as: :content %>
</ul>
<% else %>
  <p>There aren't any yet. You should probably submit one (or two).</p>
<% end %>
