<%= form_for [@link, @content], html: { class: 'form-horizontal' } do |f| %>
  <% if @content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content.errors.count, "error") %> prohibited this Tweet submission from being saved:</h2>

      <ul>
      <% @content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='control-group'>
    <%= f.label :body, class: 'control-label' %>
    <div class="controls">
      <%= f.text_area :body %>
    </div>
  </div>

  <div class="controls">
    <%= f.submit "Suggest Tweet" %>
  </div>
<% end %>
